workflow:
  tasks:
    task_prep:
      partition: "{{ platform.partition.netaccess }}"
    task_forecast:
      partition: "{{ platform.partition.compute }}"
    metatask_post:
      task_post_#fhr#:
        partition: "{{ platform.partition.task }}"
