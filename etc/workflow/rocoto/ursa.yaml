workflow:
  tasks:
    task_prep:
      partition: "{{ app.platform.partition.netaccess }}"
    task_forecast:
      partition: "{{ app.platform.partition.compute }}"
    metatask_post:
      task_post_#fhr#:
        partition: "{{ app.platform.partition.task }}"
