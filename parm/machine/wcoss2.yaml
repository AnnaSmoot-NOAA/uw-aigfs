platform:
  scheduler: pbs
  pretrained_model_path: '{{ user.aigfs_app }}/fix'

prep:
  aigfs_ics:
    execution:
      envcmds:
        - module load wflow_wcoss

forecast:
  graphcast_model:
    execution:
      envcmds:
        - module load wflow_wcoss
